---
name: CI

on:
  pull_request:
    types:
      - edited
      - opened
      - reopened
      - synchronize

jobs:
  setup-action-explicit:
    name: Setup Action Explicit
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup
        uses: ./setup
        with:
          elixir-version: 1.15.4-otp-25
          otp-version: 25.3.2.5

      - name: Check
        run: elixir --version

  setup-action-tools-file:
    name: Setup Action Tools File
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create Tools File
        run: |
          touch .tool-versions
          echo "elixir 1.15.4-otp-25" >> .tool-versions
          echo "erlang 25.3.2.5" >> .tool-versions
          cat .tool-versions

      - name: Setup
        uses: ./setup

      - name: Check
        run: elixir --version
